[build]
builder = "NIXPACKS"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @ctf/web run build"

[deploy]
startCommand = "pnpm --filter @ctf/web run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

